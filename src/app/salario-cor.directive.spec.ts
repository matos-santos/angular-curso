import { SalarioCorDirective } from './salario-cor.directive';

describe('SalarioCorDirective', () => {
  it('should create an instance', () => {
    const directive = new SalarioCorDirective();
    expect(directive).toBeTruthy();
  });
});
